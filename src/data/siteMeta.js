/**
 * Portfolio “launch” anchor for the visitor stats card (ISO `YYYY-MM-DD`).
 * Change to the date you first shipped this site.
 */
export const SITE_LAUNCH_DATE = "2026-03-30";

/**
 * Added to the displayed visitor count only (Redis / API raw count unchanged).
 * Set to 0 unless you want to offset the public number.
 */
export const VISITOR_COUNT_DISPLAY_OFFSET = 0;
